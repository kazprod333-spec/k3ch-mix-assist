# Stubs FX Chains K3CH

Chaque sous-dossier porte le **nom logique** de la chaîne.
Il ne contient pas de fichier `.multipreset` Studio Pro (format propriétaire,
à créer dans l’hôte).

1. Ouvre `NOTES.txt`.
2. Recrée les inserts sur un canal test.
3. **Store FX Chain** sous le nom indiqué.
4. Lie une macro (voir `../Macros/README.md`).
